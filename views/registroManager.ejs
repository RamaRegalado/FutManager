<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    <title>FutManager</title>
</head>
<body>
    <!-- --------------------HEADER------------------ -->
    <header>
        <%- include("parcial/head.ejs")%>
   
    </header>
    <!-- -----------------------BODY------------------------- -->
    
    <% if(locals.errors)   { %>
      <% for(let oneError in errors )  {%> 
      
      <p> <%= errors[oneError].msg %>  </p>
     <% } %>

     <% } %>        

        <form action="/registroManager" method="POST" enctype="multipart/form-data"    id="accion"> 
          <div>
            <div>
              <label for="full-name">nombre</label><br>
              <input type="text" class="" id="" name="full-name" placeholder="nombre completo" value="<%= locals.old && old.nombre ? old.nombre : " " %> ">
              
              </div><br>
          
            
               
              <div><label for="pais">Pais</label><br>
                <input type="text" name="pais" value="<%= locals.old && old.pais ? old.pais : " " %> ">
              </div>
              
              <div><label for="ciudad">Ciudad</label><br>
                <input type="text" name="ciudad"  value="<%= locals.old && old.ciudad ? old.ciudad : " " %> ">
								
              </div><br>
               
              <div >
                <label for="email">Email address</label><br>
                <input type="email" class="" id="" name="email" 
                placeholder="Enter email" 
                value="<%= locals.old && old.email ? old.email : " " %> ">
               
								
                </div><br>
               
                <div>
                  <label for="password">password<br>
                  <input type="password" name="password" value="<%= locals.old && old.password ? old.password : "" %>" >
               </div><br>

               

               <div>
                <label for="avatar">Ingresa foto</label><br>
                 <input type="file" id="foto" name="avatar" value="<%= locals.errors && old.avatar ? old.avatar : " " %>"  placeholder="subir foto" ><br> 
               </div>

              <button type="submit">enviar</button>
            
            </div>
        </form>
     

    
             <footer>
              <%- include("parcial/footer.ejs")%>
        
              </footer>
    </body>
</html>